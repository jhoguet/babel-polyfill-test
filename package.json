{
  "name": "babel-polyfill-test",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install && npm run build:aurelia && npm run build:babel"
  },
  "devDependencies": {
    "babel-polyfill": "^6.9.1",
    "webpack": "^1.13.1"
  }
}
